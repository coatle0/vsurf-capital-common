# Codex Reports

`#vsurf-code-reports`에서 요청된 기술 조사·코드 수정 결과를 선택적으로 저장하는 별도 로그 경로다.

- 정식 Order 번호를 사용하지 않는다.
- `orders/NNN_*.md`와 `reports/NNN_report.md`를 만들지 않는다.
- 필요 시 `YYYYMMDD_HHMM_<slug>.md` 형식을 사용한다.
- 정식 실행이 필요하면 Git Order를 새로 만들고 `#vsurf-agent-control`로 이동한다.
