[PIN-3 v2]

# VSURF Work Cycle — 5단계 프롬프트 + 출력물 템플릿

> 기준일: 2026-04-17 | 갱신: 2026-05-03 (#CT-003 v7)
> 적용 대상: 전 BU Order 실행
> 원칙: 계획 승인 전까지 코드 작성 금지. Research → Plan → 구현 순서 엄수.
> v1 → v2 변경: 자체 ID header `[PIN-3 v2]` 추가

---

## Step 1 — Order 발행 (CIO·COO / claude.ai)

### 프롬프트 (COO → CIO 제안)
```
[Order #[BU]-[번호] 발행 제안]

목표: [한 줄]
배경: [왜 지금 이 Order인가]
산출물: [파일명·경로 명시]
Gate 조건: [완료 기준]
선행 Order: [있으면 명시]
```

### 출력물
```
Order_[BU]-[번호].md
저장: 본 Project Knowledge (CT BU 의 경우)
      또는 GM 작업 공간 (BU 별)
```

---

## Step 1.5 — Research (Claude Code → GM 검토)

### Claude Code 프롬프트
```
[Research 지시 — #[BU]-[번호]]

아래 경로의 코드를 깊이, 상세히, 세부 사항까지 빠짐없이 읽어라.
각 파일의 모든 함수를 하나씩 읽어라. 대충 훑지 마라.
호출 관계, 인자, 반환값, 사이드이펙트까지 세부 사항을 기록하라.
추측하지 말 것. 코드에 없으면 없다고 기록하라.

읽을 대상:
- [코드 경로 1]
- [코드 경로 2]
- [관련 config 파일]

분석 후 research.md를 작성하라.
아직 구현하지 마라. Research만 한다.

research.md 포함 항목:
1. 현재 구조 요약 (파일·함수·호출 관계)
2. Order 목표와 관련된 기존 코드 현황
3. 수정·추가가 필요한 파일 목록 (추정)
4. 발견된 이슈·공백·주의사항
5. 구현 시 영향받는 의존성

저장: [BU 코드 경로]\research.md
```

### GM 검토 프롬프트 (claude.ai)
```
Claude Code가 생성한 research.md를 읽어라.
Know의 [관련 handover]도 함께 읽어라.

검토 후 아래를 확인하라:
- 누락된 파일·함수가 있는가
- 기존 handover와 충돌하는 내용이 있는가
- 추가해야 할 컨텍스트가 있는가

수정사항을 research.md에 직접 반영하고 COO에게 보고하라.
```

### 출력물 템플릿 (research.md)
```markdown
# Research — Order #[BU]-[번호]
> 생성: Claude Code | 검토: GM | 날짜: YYYY-MM-DD

## 1. 현재 구조
[파일·함수·호출 관계 — 코드 기반 사실만]

## 2. Order 관련 현황
[목표 달성에 필요한 기존 코드 현황]

## 3. 수정·추가 필요 파일
| 파일 | 경로 | 필요 작업 |
|------|------|-----------|

## 4. 이슈·공백·주의사항
[코드에서 발견된 문제. 추측 금지.]

## 5. 의존성
[영향받는 파일·함수·외부 시스템]
```

---

## Step 1.7 — Plan 작성·승인 (GM → COO·CIO / claude.ai)

### GM 프롬프트 (claude.ai)
```
research.md를 기반으로 plan.md를 작성하라.
아직 구현하지 마라. Plan만 작성한다.

plan.md 포함 항목:
1. 구현 접근 방식 (어떤 순서로 무엇을 할 것인가)
2. 수정될 파일 전체 목록 + 각 파일에서 할 작업
3. 신규 생성 파일 목록 + 역할
4. 핵심 함수 스펙 (입력·출력·로직 요약)
5. 테스트·검증 방법
6. 리스크 (잘못될 수 있는 것)

저장: [BU orders 경로]\plan_[BU]-[번호].md
작성 후 COO에게 검토 요청하라.
```

### COO 검토 기준 (claude.ai 내부)
```
- research와 plan이 일치하는가
- 범위가 Order를 초과하지 않는가
- 리스크가 명시되어 있는가
- Gate 조건을 충족할 수 있는 plan인가
- CRO Rules 위반 가능성 없는가
```

### 출력물 템플릿 (plan.md)
```markdown
# Plan — Order #[BU]-[번호]
> 작성: GM | COO 승인: [ ] | CIO 승인: [ ] | 날짜: YYYY-MM-DD

## 1. 구현 접근 방식
[단계별 순서. 구체적으로.]

## 2. 수정 파일 목록
| 파일 | 경로 | 작업 내용 |
|------|------|-----------|

## 3. 신규 생성 파일
| 파일 | 경로 | 역할 |
|------|------|------|

## 4. 핵심 함수 스펙
### [함수명]
- 입력:
- 출력:
- 로직:

## 5. 테스트·검증
[완료 기준 + 검증 방법]

## 6. 리스크
[잘못될 수 있는 것 + 대응]

---
COO 승인: [ ] — [코멘트]
CIO 승인: [ ] — [코멘트]
```

---

## Step 2 — 구현 (Claude Code → GM 검토)

### Claude Code 프롬프트
```
[구현 지시 — #[BU]-[번호]]

plan.md를 읽어라. 승인된 plan대로만 구현하라.
plan에 없는 것은 구현하지 마라.
구현 중 plan과 다른 상황이 발생하면 즉시 멈추고 보고하라.

구현 순서:
1. plan.md §2 수정 파일 순서대로 진행
2. 각 파일 완료 시 콘솔에 체크 출력
3. §5 테스트·검증 실행
4. 전체 완료 후 결과 요약 출력

방향이 틀어졌다면 패치하지 말고 즉시 멈춰라.
git reset 후 범위를 좁혀 재시작한다.

산출물 저장:
- 구현 파일: [코드 경로]
- Report: [BU reports 경로]\Order_[BU]-[번호]_report.md
```

### GM 검토 프롬프트 (claude.ai)
```
Claude Code 구현 결과를 검토하라.
plan.md와 대조하여 아래를 확인하라:

- plan 대비 누락된 항목이 있는가
- 예상치 못한 변경이 있는가
- Gate 조건이 충족됐는가
- 다음 Order에 영향을 주는 사항이 있는가

확인 후 handover를 갱신하고 COO에게 제출하라.
```

### 출력물 템플릿 (Order_report.md)
```markdown
# Order #[BU]-[번호] — Report
> 완료일: YYYY-MM-DD | 담당: [BU] GM

## 1. 완수 여부
Y / N (부분 완수 시 이유 명시)

## 2. 산출물
| 파일명 | 경로 | 상태 |
|--------|------|------|

## 3. Gate 조건 충족
| 조건 | 결과 |
|------|------|

## 4. 증빙
[테스트 결과·로그·스크린샷 경로]

## 5. 다음 Order 영향
[후속 Order에 전달할 사항]
```

---

## Step 3 — 최종 승인 (COO·CIO / claude.ai)

### COO 검토 프롬프트 (claude.ai 내부)
```
[BU] GM handover + Order_report.md를 검토하라.

확인 항목:
- Gate 조건 전항목 충족 여부
- CRO Rules 위반 없는가
- 다음 Order 선행 조건 충족됐는가
- 공백·리스크 잔존 여부

이상 없으면 CIO에게 승인 요청.
문제 있으면 GM에게 재작업 지시.
```

### 출력물
```
Order_[BU]-[번호]_done.md  ← 완료 마킹 (rename)
CT_handover.md 갱신
```

---

## 프롬프트 공통 원칙

| 원칙 | 적용 단계 | 표현 |
|------|-----------|------|
| 깊이 읽기 | Step 1.5 | "깊이, 상세히, 세부 사항까지 빠짐없이" |
| 구현 차단 | Step 1.5·1.7 | "아직 구현하지 마라" |
| 사실 기반 | Step 1.5 | "추측하지 말 것. 코드에 없으면 없다고 기록" |
| 범위 준수 | Step 2 | "plan에 없는 것은 구현하지 마라" |
| 이탈 시 중단 | Step 2 | "방향이 틀어지면 패치하지 말고 즉시 멈춰라" |
| 단일 세션 | Step 1.5→2 | Claude Code 세션 끊지 않고 이어감 |

---

═══════════════════════════════════════════
*— VSURF Work Cycle v2 (PIN-3 v2) | 2026-05-03 —*
*— v1 → v2: 자체 ID header 추가 —*
═══════════════════════════════════════════
